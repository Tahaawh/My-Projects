# School Management

School management project: contains menu, main, and helper modules.

How to run
- Open `main.py` and follow the program prompts (requires Python 3).

Files
- `main.py`, `menus.py`, `defs.py`, `data.json`